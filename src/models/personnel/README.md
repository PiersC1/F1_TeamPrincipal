# Personnel Models

Contains staff representations.

*   `staff.py`: Base object for anyone employed by the team, possessing a rating and salary.
*   `driver.py`: Inherits from `StaffMember` and includes specific on-track statistics used by the `RaceSimulator`.
